function slammygroundtruthtwoloops = importfile(filename, dataLines)
%IMPORTFILE Import data from a text file
%  SLAMMYGROUNDTRUTHTWOLOOPS = IMPORTFILE(FILENAME) reads data from text
%  file FILENAME for the default selection.  Returns the numeric data.
%
%  SLAMMYGROUNDTRUTHTWOLOOPS = IMPORTFILE(FILE, DATALINES) reads data
%  for the specified row interval(s) of text file FILENAME. Specify
%  DATALINES as a positive scalar integer or a N-by-2 array of positive
%  scalar integers for dis-contiguous row intervals.
%
%  Example:
%  slammygroundtruthtwoloops = importfile("D:\Philipp\5-Projects\rikirobot\slammy\Groundtruth\slammy_ground_truth_two_loops.txt", [2, Inf]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 27-Jul-2021 14:26:20

%% Input handling

% If dataLines is not specified, define defaults
if nargin < 2
    dataLines = [2, Inf];
end

%% Set up the Import Options and import the data
opts = delimitedTextImportOptions("NumVariables", 7);

% Specify range and delimiter
opts.DataLines = dataLines;
opts.Delimiter = ",";

% Specify column names and types
opts.VariableNames = ["Xm", "Ym", "Headingdeg", "Var4", "Var5", "Var6", "Var7"];
opts.SelectedVariableNames = ["Xm", "Ym", "Headingdeg"];
opts.VariableTypes = ["double", "double", "double", "string", "string", "string", "string"];

% Specify file level properties
opts.ExtraColumnsRule = "ignore";
opts.EmptyLineRule = "read";

% Specify variable properties
opts = setvaropts(opts, ["Var4", "Var5", "Var6", "Var7"], "WhitespaceRule", "preserve");
opts = setvaropts(opts, ["Var4", "Var5", "Var6", "Var7"], "EmptyFieldRule", "auto");
opts = setvaropts(opts, ["Xm", "Ym", "Headingdeg"], "TrimNonNumeric", true);
opts = setvaropts(opts, ["Xm", "Ym", "Headingdeg"], "DecimalSeparator", ",");
opts = setvaropts(opts, ["Xm", "Ym", "Headingdeg"], "ThousandsSeparator", ".");

% Import the data
slammygroundtruthtwoloops = readtable(filename, opts);

%% Convert to output type
slammygroundtruthtwoloops = table2array(slammygroundtruthtwoloops);
end